{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API service",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'tsx' src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate:latest": "knex --knexfile src/db/knexfile.ts migrate:latest",
    "migrate:up": "knex --knexfile src/db/knexfile.ts migrate:up",
    "migrate:down": "knex --knexfile src/db/knexfile.ts migrate:rollback",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down -v",
    "db:logs": "docker-compose logs -f",
    "db:restart": "npm run db:down && npm run db:up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "knex": "^3.1.0",
    "mysql2": "^3.11.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.2",
    "nodemon": "^3.1.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
